import type { ResumeSkills } from "lib/redux/types";
export declare const ResumePDFSkills: ({ heading, skills, themeColor, showBulletPoints, }: {
    heading: string;
    skills: ResumeSkills;
    themeColor: string;
    showBulletPoints: boolean;
}) => import("react").JSX.Element;
