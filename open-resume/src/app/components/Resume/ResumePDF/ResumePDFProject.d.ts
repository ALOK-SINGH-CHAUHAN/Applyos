import type { ResumeProject } from "lib/redux/types";
export declare const ResumePDFProject: ({ heading, projects, themeColor, }: {
    heading: string;
    projects: ResumeProject[];
    themeColor: string;
}) => import("react").JSX.Element;
