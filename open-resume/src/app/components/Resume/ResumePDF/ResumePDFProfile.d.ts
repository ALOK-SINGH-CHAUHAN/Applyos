import type { ResumeProfile } from "lib/redux/types";
export declare const ResumePDFProfile: ({ profile, themeColor, isPDF, }: {
    profile: ResumeProfile;
    themeColor: string;
    isPDF: boolean;
}) => import("react").JSX.Element;
